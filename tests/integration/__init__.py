"""Integration suite.

A package (rather than a bare directory) so ``tests.integration.eagar_tsai``,
the analytical reference imported by ``test_validation.py``, resolves the same
way under any pytest import mode.
"""
